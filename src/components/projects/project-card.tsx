import Image from "next/image";
type Props={project:{slug:string;tag:string;title:string;image:string;cardHeight:number;imageHeight:number}};
export function ProjectCard({ project }:Props) { return <a href={`/proyectos/${project.slug}`} style={{ height: project.cardHeight }} className="flex w-[300px] flex-col border-2 border-white bg-[var(--color-bg-surface)]"><Image src={project.image} alt={`Vista previa del proyecto ${project.title}`} width={300} height={project.imageHeight} style={{ height: project.imageHeight }} className="w-full object-cover"/><div className="flex flex-1 flex-col gap-3 p-4"><span className="w-fit border border-[var(--color-accent-border)] bg-[var(--color-accent-subtle)] px-3 py-1 text-[10px] text-[var(--color-accent)]">{project.tag}</span><h3 className="font-display text-2xl leading-[31px] tracking-[-.5px]">{project.title}</h3><span className="ml-auto mt-auto border border-[var(--color-accent-border)] bg-[var(--color-accent-subtle)] px-[18px] py-[3px] text-[var(--color-accent)]">→</span></div></a>; }
